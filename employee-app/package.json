{
  "name": "employee-app",
  "version": "1.0.0",
  "description": "Employee Mobile App wrapper for the Restaurant Management System",
  "private": true,
  "scripts": {
    "build:web": "cd ../frontend && npm run build",
    "sync": "npx cap sync",
    "build:android": "npm run build:web && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.8",
    "@capacitor/core": "^5.7.8"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.8"
  }
}
