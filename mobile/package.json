{
  "name": "restaurant-customer-mobile",
  "version": "1.0.0",
  "description": "Customer Mobile App wrapper for the Restaurant Management System",
  "private": true,
  "scripts": {
    "build:web": "cd ../customer-web && npm run build",
    "sync": "npx cap sync",
    "build:android": "npm run build:web && npx cap sync android",
    "build:ios": "npm run build:web && npx cap sync ios",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "@capacitor/ios": "^8.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.6"
  }
}
